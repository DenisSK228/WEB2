<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {{#if title }}
        {{{title}}}
        {{else}}
        Название этого сайта
        {{/if}}
    </title>
</head>

<body>
    <h1>Hello, {{{ world }}}</h1><br>

    <h2>Привет {{{ name }}} ростом {{{ height }}}м {{{ yo }}} года рождения</h2>
    {{{ img }}}
    <h3>Максу не хватает ни на что из этого:
        <ul>
            {{#each list}}
            <li>{{this}}</li>
            {{/each}}
        </ul>

        <ul>
            {{#each keysd}}
            <li>{{@key}} - {{this}}</li>
            {{/each}}
        </ul>
    </h3>

</body>

</html>